# requirements.txt
fastapi==0.111.0
uvicorn[standard]==0.30.1
Jinja2==3.1.4
python-dotenv==1.0.1

SQLAlchemy==2.0.32
psycopg[binary]==3.2.1      # driver v3
psycopg2-binary==2.9.9      # keep as fallback; SQLAlchemy will use psycopg2 for postgresql://

pydantic==2.8.2
email-validator==2.2.0      # needed for pydantic EmailStr
python-multipart==0.0.9     # form uploads
aiofiles==23.2.1            # Starlette StaticFiles helper
